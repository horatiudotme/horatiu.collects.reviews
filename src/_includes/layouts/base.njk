<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ seoTitle }}</title>
  <meta name="description" content="{{ seoDescription }}">

  <!-- Load the Roboto and Material Icons fonts. -->
  {# <link href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,700" rel="stylesheet"> #}
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Material+Icons&display=block" rel="stylesheet">

  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <mwc-drawer hasHeader type="modal">
    <span slot="title">{{ metadata.title }}</span>
    <span slot="subtitle">what others are saying about my apps (keep me going forward)</span>

    {% include "components/common/drawer-content.njk" %}

    <div slot="appContent">
      <mwc-top-app-bar-fixed centerTitle>
        <mwc-icon-button slot="navigationIcon" icon="menu"></mwc-icon-button>
        <div slot="title">{{ metadata.title }}</div>
        <mwc-icon-button id="shuffleButton" slot="actionItems" icon="sort"></mwc-icon-button>

        <mwc-tab-bar id="reviewTypeTabBar">
          <mwc-tab label="all"></mwc-tab>
          <mwc-tab label="media"></mwc-tab>
          <mwc-tab label="user"></mwc-tab>
        </mwc-tab-bar>
      </mwc-top-app-bar-fixed>
      <div>
        <!-- content -->
        <main class="hcr-content-wrapper">
          {{ content | safe }}
        </main>
      </div>
    </div>
  </mwc-drawer>

  <script src="/assets/js/main.js"></script>
</body>

</html>